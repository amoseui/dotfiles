# superpowers 작업 산출물 (spec/plan)은 private/docs/superpowers/에 커밋해 보관한다.
# 실수로 public 쪽에 생성된 산출물이 커밋되지 않도록 가드로 유지.
docs/superpowers/
.claude/worktrees/

# Hermes가 repo 안에 쓰는 로컬 계획 파일 — private/docs/plans/로 이동해 보관
.hermes/

# 에이전트 도구가 repo 루트에 떨어뜨리는 배치·산출물 파일 (예: aside/suno)
.aside-*
artifacts/

# Python 캐시
__pycache__/
*.pyc
.DS_Store

# 어떤 환경 파일도 커밋하지 않는다
**/.env
**/.env.*
!**/.env.example
